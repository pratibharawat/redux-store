function createStore() {
    // the store have four parts:
    // the state
    // get the state
    // listen to changes in state
    // update the state

    let state;
    const getState = () => state
        return {
        getState
    };  
}